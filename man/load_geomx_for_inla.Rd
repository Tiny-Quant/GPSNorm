% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_data.R
\name{load_geomx_for_inla}
\alias{load_geomx_for_inla}
\title{Load a GeoMx DSP export for INLA-style modeling}
\usage{
load_geomx_for_inla(
  path_xlsx,
  seg_covariates = c("AOISurfaceArea", "AOINucleiCount", "DeduplicatedReads",
    "SequencingSaturation", "Mix")
)
}
\arguments{
\item{path_xlsx}{Character scalar. Path to the GeoMx Excel file.}

\item{seg_covariates}{Character vector. Names of segment/AOI-level covariates
to carry forward from the \code{SegmentProperties} sheet. These columns must be
present in the sheet. Defaults to common sequencing/QC covariates.}
}
\value{
Long-form AOI-by-gene data (\code{tibble}) including counts, IDs,
coordinates, requested covariates, and derived indicators.
}
\description{
Reads a NanoString GeoMx DSP Excel export (xlsx) containing the sheets
\code{"SegmentProperties"}, \code{"TargetProperties"}, and \code{"TargetCountMatrix"}, and
returns:
(i) an AOI-level table, (ii) a target/gene-level table, and (iii) a long-form
AOI-by-gene count table with numeric IDs and common derived fields.
}
\details{
This function is intended as a preprocessing step for downstream models
(e.g., INLA), but does not require INLA itself.

Required columns:
\itemize{
\item \code{SegmentProperties}: \code{SlideName}, \code{ROILabel}, \code{SegmentDisplayName},
\code{ROICoordinateX}, \code{ROICoordinateY}, plus any \code{seg_covariates}.
\item \code{TargetProperties}: \code{TargetName}, \code{CodeClass}.
\item \code{TargetCountMatrix}: a \code{TargetName} column; remaining columns
correspond to \code{SegmentDisplayName}.
}

\code{CodeClass_clean} is mapped as:
\itemize{
\item \code{"Negative"} \eqn{\rightarrow} \code{"NEG"}
\item \code{"Control"} \eqn{\rightarrow} \code{"HK"} (housekeeping)
\item \code{"Endogenous"} \eqn{\rightarrow} \code{"REG"}
}
}
\examples{
\dontrun{
dat <- load_geomx_for_inla(
    "geomx.xlsx",
    seg_covariates = c("AOISurfaceArea", "AOINucleiCount")
)
head(dat$long)
}

}
