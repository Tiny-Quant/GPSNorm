% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{canonicalize_spe_inputs}
\alias{canonicalize_spe_inputs}
\title{Canonicalize a SpatialExperiment for benchmarking simulation.}
\usage{
canonicalize_spe_inputs(
  spe,
  gene_type_col = NULL,
  group_col = NULL,
  domain_col = NULL,
  counts_assay = "counts",
  prop_neg = 0.05,
  prop_hk = 0.05,
  k_domains = 7,
  seed = 1
)
}
\arguments{
\item{spe}{A SpatialExperiment with a 'counts' assay.}

\item{gene_type_col}{Optional character. If present in rowData(spe), used as gene type.}

\item{group_col}{Optional character. If present in colData(spe), used as group label.}

\item{domain_col}{Optional character. If present in colData(spe), used as domain label.}

\item{counts_assay}{Character. Assay name for counts. Default "counts".}

\item{prop_neg, prop_hk}{Fractions of genes to label NEG/HK if missing.}

\item{k_domains}{Integer number of domains (only used if domain labels missing).}

\item{seed}{Integer seed for reproducibility.}
}
\value{
A list with:
\itemize{
\item \code{spe}: SpatialExperiment with added \code{rowData(spe)$gene_type} and/or
\code{colData(spe)$sim_group}, \code{colData(spe)$sim_domain} as needed.
\item \code{meta}: list describing which fields were created and key summaries.
}
}
\description{
Ensures the baseline \code{spe} has:
(i) a per-gene type label in {NEG, HK, REG},
(ii) a per-AOI binary group label (for DE),
(iii) a per-AOI spatial domain/cluster label (for ARI benchmarking).
}
\details{
If any are missing, they are constructed from the observed counts and/or
spatial coordinates using deterministic rules (given \code{seed}).
}
