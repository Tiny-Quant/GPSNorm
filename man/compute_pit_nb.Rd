% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{compute_pit_nb}
\alias{compute_pit_nb}
\title{Randomized PIT for Negative Binomial observations}
\usage{
compute_pit_nb(y, mu, size, randomized = TRUE, rng = NULL)
}
\arguments{
\item{y}{Integer vector of observed counts (>= 0).}

\item{mu}{Numeric vector of means (> 0).}

\item{size}{Numeric vector of NB size parameters (> 0).}

\item{randomized}{Logical; if TRUE (default), apply uniform randomization
within the probability mass at y.}

\item{rng}{Optional numeric vector in (0,1) used for randomization.
If NULL and randomized=TRUE, values are drawn via runif().}
}
\value{
Numeric vector u in (0, 1), same length as y.
}
\description{
Computes a randomized probability integral transform (PIT) for observations
from a Negative Binomial distribution. Randomization is required because the
NB distribution is discrete.
}
\details{
The PIT is computed as:
u = F(y - 1) + v * P(Y = y),
where v ~ Uniform(0,1).

This ensures u is continuous even for discrete distributions.
}
